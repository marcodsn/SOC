{#- ───────────────────────────────────────────────────────────────────────────
    Experience Generation Prompt — split for prompt caching

    This template is rendered ONCE per call and inserted as the **user**
    message.  The static instruction block now lives in the system message
    (assembled in ExperienceGenerator._EXPERIENCE_SYSTEM_PROMPT), so this
    file contains ONLY the variable, per-call content: the persona pair,
    few-shot examples, and any diversity hints.

    The system message (built in code) contains:
      • Role definition
      • Full output format specification (<experience> XML)
      • All requirements (relationship variety, cadence rules, etc.)
      • Language and channel constraints

    This split maximises the byte-identical prefix across generation calls
    so that providers with automatic prompt caching can reuse the KV cache.
─────────────────────────────────────────────────────────────────────────── -#}
## Personas

The following two personas will be having a conversation. You can see both — they cannot see each other's profile.

{% for persona in personas %}
{{ persona.persona_text }}

{% endfor %}

---

## Example Experience(s)

Use the example(s) below as a template and inspiration, but create a new experience that is distinctly different while maintaining realism and depth.

{% for shot in shots %}
{{ shot }}

{% endfor %}

---

## Generate

{% if style_hint %}
**Diversity note:** Recent generations have over-represented certain styles. Please lean toward generating a **{{ style_hint }}** conversation for this experience.

{% endif %}
{% if language_hint %}
**Language:** The shared lingua franca for this conversation is **{{ language_hint }}**. Both personas communicate in this language, only occasionally sprinkling in a native slang word or greeting for flavor.

{% endif %}
Create ONE realistic experience bringing these two personas into conversation. Output ONLY the `<experience>` block — no additional commentary.
