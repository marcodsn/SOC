{#- ───────────────────────────────────────────────────────────────────────────
    Persona Generation Prompt — split for prompt caching

    This template is rendered ONCE per call and inserted as the **user**
    message.  The static instruction block now lives in the system message
    (assembled in PersonaGenerator), so this file contains ONLY the
    variable, per-call content: the few-shot examples, the target persona
    metadata, and a short reminder of the task.

    The system message (built in code) contains:
      • Role definition
      • Full format specification with XML/markdown structure
      • All diversity guidelines
      • All stylistic rules (show-don't-tell, positive framing, etc.)

    This split maximises the byte-identical prefix across generation calls
    so that providers with automatic prompt caching can reuse the KV cache.
─────────────────────────────────────────────────────────────────────────── -#}
## Example Persona(s)

Use the example(s) below as a template and inspiration, but create a new persona that is distinctly different in multiple dimensions while maintaining psychological realism and depth.

{% for shot in shots %}
{{ shot }}

{% endfor %}

---

## Your Task

Generate ONE new persona with these details:
- **Name:** {{ persona_info["name"] }}
- **Age:** {{ persona_info["age"] }}
- **Region:** {{ persona_info["region"] }}
- **Location:** {{ persona_info["subregion"] }}
{% if persona_info.get("lingua_franca") %}
- **Lingua franca:** {{ persona_info["lingua_franca"] }} (example messages should be in this language; native slang is fine sparingly)
{% endif %}

The persona must:
1. Follow the exact XML + markdown format from the instructions
2. Be distinctly different from the example(s) in multiple dimensions
3. Maintain psychological realism and depth
4. Include specific, concrete details (actual hobbies, real communication patterns, named relationships)
5. Have conversational speech patterns with quoted example phrases
6. Represent "normal" people — not extreme cases
7. Be 2000–3000 characters total

Begin now with the `<character>` opening tag. Output ONLY the persona between `<character>` and `</character>` tags.
